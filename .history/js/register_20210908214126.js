function Validator(options) {
  //   console.log(option);
  const formElement = document.querySelector(optiosn.form);
  if (formElement) {
    console.log(formElement);
  }
}
// Validator(option);
// // định nghĩa rule
Validator.isRequired = function (seletor) {
  console.log(seletor);
};
Validator.isEmail = function (seletor) {
  console.log(seletor);
};
